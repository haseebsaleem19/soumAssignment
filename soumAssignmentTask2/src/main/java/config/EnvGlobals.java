package config;

/**
 * EnvGlobals class holds global environment variables.
 */
public class EnvGlobals {
    /**
     * Authorization token for authentication.
     * Example: Bearer token
     */
    public static String authorizationToken;
}
